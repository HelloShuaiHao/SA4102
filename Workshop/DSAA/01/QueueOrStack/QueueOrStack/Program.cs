namespace QueueOrStack;

public class Solution
{
    public static void Main()
    {
        Console.WriteLine(CheckBalance.Check("a {b [c (d + e)/2 - f  ] + 1\n"));
        Console.ReadKey();
        // {[()])}
    }
}