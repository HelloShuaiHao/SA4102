namespace HClass;

public class Exercise_H
{
    static int[] arr = new int[] { 1, 2, 3, 4, 5 };

    //
    delegate double doubleOps(double x);

    public static void Main()
    {
        // int num = H1.ReadInteger();
        // H2.PrintArray(arr);
        // Console.WriteLine(H3.Substitute("SHUAIHAO", 'A', 'V'));

        //H4.SetArray(ref arr, 8);
        //for (int i = 0; i < arr.Length; i++) Console.WriteLine(arr[i] + " ");

        //int[] ans = H5.ResizeArray(arr, 9);
        //for (int i = 0; i < arr.Length; i++) Console.WriteLine(arr[i] + " ");



        /*
        for(int i = 5; i <= 1000;i++)
        {
            if (H6.isPrimte(i)) Console.WriteLine(i);
        }*/



        /*
        int[,] A = new int[4, 2]
        {
            {1,2},
            {3,4},
            {5,6},
            {7,8},
        };
        int[,] B = new int[2, 3]
        {
            {9,10,11},
            {11,12,13},
        };
        int[,] ans = new int[4, 3];
        ans = H7.MatricMultiply(A, B);
        for(int i = 0; i < 4; i++)
        {
            for(int j = 0; j < 3; j++)
            {
                Console.Write(ans[i, j] + " ");
            }
            Console.Write("\n");
        }*/

    }
}